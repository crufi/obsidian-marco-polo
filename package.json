{
	"name": "obsidian-marco-polo",
	"version": "0.1.3",
	"description": "Obsidian plugin: filesystem path completion, validation, and reveal.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "node --test --disable-warning=ExperimentalWarning --disable-warning=MODULE_TYPELESS_PACKAGE_JSON \"test/**/*.test.ts\""
	},
	"keywords": [],
	"author": "Steve Crutchfield (https://github.com/crufi)",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.28.0",
		"obsidian": "latest",
		"typescript": "^5.4.0"
	}
}
